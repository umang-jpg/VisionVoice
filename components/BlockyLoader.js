// BlockyLoader is superseded by WaveformLoader.
// Kept as a stub so any stale import doesn't crash the bundler.
import React from 'react';
import { View } from 'react-native';
export default function BlockyLoader() {
  return <View />;
}

